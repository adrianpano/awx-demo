---
- name: IOS facts
  hosts: gns3_router_group
  gather_facts: true
  strategy: free
  tasks:
    - name: Print ansible_facts
      ansible.builtin.debug:
        msg: 
          - "{{ ansible_facts}}"

